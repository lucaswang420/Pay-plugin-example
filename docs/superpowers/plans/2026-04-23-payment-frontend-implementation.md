# Payment Test Frontend Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a Vue 3 wizard-style test interface for Alipay sandbox payment flows with 5 steps: create order → select payment method → pay → refund → view refund status.

**Architecture:** Single-page Vue 3 application using Vite for development, Pinia for state management, Vue Router for wizard navigation, and Axios for API communication. Frontend runs independently in development with Vite proxy to backend, deploys via Docker Compose with nginx reverse proxy.

**Tech Stack:** Vue 3 (Composition API), Vite 7, Vue Router 4, Pinia 3, Axios 2, Docker

---

## Task 1: Create Frontend Project Structure

**Files:**
- Create: `PayFrontend/package.json`
- Create: `PayFrontend/index.html`
- Create: `PayFrontend/vite.config.js`
- Create: `PayFrontend/.gitignore`
- Create: `PayFrontend/README.md`

- [ ] **Step 1: Create package.json**

```bash
mkdir -p PayFrontend
cd PayFrontend
```

Create `package.json`:
```json
{
  "name": "pay-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.24",
    "vue-router": "^4.6.4",
    "pinia": "^3.0.5",
    "axios": "^1.8.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.2.4"
  }
}
```

- [ ] **Step 2: Create index.html**

Create `index.html`:
```html
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alipay Sandbox Payment Test</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.js"></script>
  </body>
</html>
```

- [ ] **Step 3: Create vite.config.js**

Create `vite.config.js`:
```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8088',
        changeOrigin: true
      }
    }
  }
})
```

- [ ] **Step 4: Create .gitignore**

Create `.gitignore`:
```
# Logs
logs
*.log
npm-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage
.grunt

# Bower dependency directory
bower_components

# node_modules
node_modules/

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env
.env.test
.env.local

# parcel-bundler cache
.cache
.parcel-cache

# Vite
dist/
dist-ssr/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
```

- [ ] **Step 5: Create README.md**

Create `README.md`:
```markdown
# Payment Test Frontend

Frontend test interface for Alipay sandbox payment flows.

## Development

\`\`\`bash
npm install
npm run dev
\`\`\`

Access at http://localhost:5173

## Build

\`\`\`bash
npm run build
\`\`\`

Output: `dist/`
```

- [ ] **Step 6: Create source directory structure**

```bash
mkdir -p src/{views,components,router,api,stores,utils}
touch src/{main.js,App.vue}
```

- [ ] **Step 7: Commit project initialization**

```bash
cd PayFrontend
git init
git add .
git commit -m "feat: initialize Vue 3 frontend project structure"
```

---

## Task 2: Create Application Entry Point

**Files:**
- Create: `PayFrontend/src/main.js`
- Create: `PayFrontend/src/App.vue`

- [ ] **Step 1: Create main.js entry file**

Create `src/main.js`:
```javascript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'
import router from './router'

const app = createApp(App)

app.use(createPinia())
app.use(router)

app.mount('#app')
```

- [ ] **Step 2: Create App.vue root component**

Create `src/App.vue`:
```vue
<template>
  <div id="app">
    <header>
      <h1>Alipay Sandbox Payment Test</h1>
    </header>
    <main>
      <router-view />
    </main>
    <footer>
      <p>Test interface for Alipay sandbox payment flows</p>
    </footer>
  </div>
</template>

<script setup>
// Root component - no additional logic needed
</script>

<style>
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
  background-color: #ffffff;
  color: #000000;
}

#app {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

header {
  background-color: #f5f5f5;
  padding: 20px;
  text-align: center;
  border-bottom: 1px solid #e0e0e0;
}

header h1 {
  font-size: 24px;
  font-weight: bold;
}

main {
  flex: 1;
  max-width: 800px;
  width: 100%;
  margin: 0 auto;
  padding: 40px 20px;
}

footer {
  background-color: #f5f5f5;
  padding: 20px;
  text-align: center;
  border-top: 1px solid #e0e0e0;
  font-size: 14px;
  color: #666666;
}
</style>
```

- [ ] **Step 3: Commit application entry point**

```bash
git add src/main.js src/App.vue
git commit -m "feat: add application entry point and root component"
```

---

## Task 3: Configure Vue Router

**Files:**
- Create: `PayFrontend/src/router/index.js`

- [ ] **Step 1: Create router configuration**

Create `src/router/index.js`:
```javascript
import { createRouter, createWebHistory } from 'vue-router'

const routes = [
  {
    path: '/',
    redirect: '/step/1'
  },
  {
    path: '/step/:stepNumber',
    name: 'step',
    component: () => import('../views/StepView.vue'),
    props: true
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

export default router
```

- [ ] **Step 2: Create placeholder StepView component**

Create `src/views/StepView.vue`:
```vue
<template>
  <div>
    <h2>Step {{ stepNumber }}</h2>
    <p>Step views will be implemented in subsequent tasks</p>
  </div>
</template>

<script setup>
const props = defineProps({
  stepNumber: {
    type: String,
    required: true
  }
})
</script>
```

- [ ] **Step 3: Test router configuration**

```bash
npm install
npm run dev
```

Expected: Visit http://localhost:5173, should redirect to /step/1 and show "Step 1"

- [ ] **Step 4: Commit router configuration**

```bash
git add src/router src/views/StepView.vue
git commit -m "feat: configure Vue Router with step navigation"
```

---

## Task 4: Create Pinia Stores

**Files:**
- Create: `PayFrontend/src/stores/order.js`
- Create: `PayFrontend/src/stores/payment.js`

- [ ] **Step 1: Create order store**

Create `src/stores/order.js`:
```javascript
import { defineStore } from 'pinia'
import { ref } from 'vue'

export const useOrderStore = defineStore('order', () => {
  const orderId = ref('')
  const productName = ref('')
  const amount = ref(0)
  const description = ref('')
  const status = ref('')
  const createdAt = ref('')

  function setOrder(order) {
    orderId.value = order.orderId || ''
    productName.value = order.productName || ''
    amount.value = order.amount || 0
    description.value = order.description || ''
    status.value = order.status || ''
    createdAt.value = order.createdAt || new Date().toISOString()

    // Persist to session storage
    sessionStorage.setItem('order', JSON.stringify({
      orderId: orderId.value,
      productName: productName.value,
      amount: amount.value,
      description: description.value,
      status: status.value,
      createdAt: createdAt.value
    }))
  }

  function loadFromSession() {
    const saved = sessionStorage.getItem('order')
    if (saved) {
      const data = JSON.parse(saved)
      orderId.value = data.orderId
      productName.value = data.productName
      amount.value = data.amount
      description.value = data.description
      status.value = data.status
      createdAt.value = data.createdAt
    }
  }

  function clear() {
    orderId.value = ''
    productName.value = ''
    amount.value = 0
    description.value = ''
    status.value = ''
    createdAt.value = ''
    sessionStorage.removeItem('order')
  }

  return {
    orderId,
    productName,
    amount,
    description,
    status,
    createdAt,
    setOrder,
    loadFromSession,
    clear
  }
})
```

- [ ] **Step 2: Create payment store**

Create `src/stores/payment.js`:
```javascript
import { defineStore } from 'pinia'
import { ref } from 'vue'

export const usePaymentStore = defineStore('payment', () => {
  const paymentMethod = ref('')
  const paymentStatus = ref('')
  const qrCodeUrl = ref('')
  const paymentUrl = ref('')

  const refundAmount = ref(0)
  const refundReason = ref('')
  const refundStatus = ref('')

  function setPaymentMethod(method) {
    paymentMethod.value = method
  }

  function setPaymentInfo(info) {
    paymentStatus.value = info.status || ''
    qrCodeUrl.value = info.qrCodeUrl || ''
    paymentUrl.value = info.paymentUrl || ''
  }

  function setRefundInfo(info) {
    refundAmount.value = info.amount || 0
    refundReason.value = info.reason || ''
    refundStatus.value = info.status || ''
  }

  function clear() {
    paymentMethod.value = ''
    paymentStatus.value = ''
    qrCodeUrl.value = ''
    paymentUrl.value = ''
    refundAmount.value = 0
    refundReason.value = ''
    refundStatus.value = ''
  }

  return {
    paymentMethod,
    paymentStatus,
    qrCodeUrl,
    paymentUrl,
    refundAmount,
    refundReason,
    refundStatus,
    setPaymentMethod,
    setPaymentInfo,
    setRefundInfo,
    clear
  }
})
```

- [ ] **Step 3: Commit stores**

```bash
git add src/stores
git commit -m "feat: add Pinia stores for order and payment state"
```

---

## Task 5: Create API Client

**Files:**
- Create: `PayFrontend/src/api/index.js`
- Create: `PayFrontend/src/api/payment.js`

- [ ] **Step 1: Create axios instance**

Create `src/api/index.js`:
```javascript
import axios from 'axios'

const apiClient = axios.create({
  baseURL: '/api',
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// Request interceptor
apiClient.interceptors.request.use(
  (config) => {
    // Add auth header if needed
    const token = sessionStorage.getItem('api_token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

// Response interceptor
apiClient.interceptors.response.use(
  (response) => {
    return response.data
  },
  (error) => {
    const message = error.response?.data?.message || error.message || 'Request failed'
    return Promise.reject(new Error(message))
  }
)

export default apiClient
```

- [ ] **Step 2: Create payment API functions**

Create `src/api/payment.js`:
```javascript
import apiClient from './index'

export const paymentApi = {
  // Create payment order
  createOrder(data) {
    return apiClient.post('/pay/create', data)
  },

  // Query order status
  queryOrder(outTradeNo) {
    return apiClient.get('/pay/query', {
      params: { out_trade_no: outTradeNo }
    })
  },

  // Request refund
  refund(data) {
    return apiClient.post('/pay/refund', data)
  },

  // Query refund status
  queryRefund(outTradeNo) {
    return apiClient.get('/pay/refund/query', {
      params: { out_trade_no: outTradeNo }
    })
  }
}
```

- [ ] **Step 3: Commit API client**

```bash
git add src/api
git commit -m "feat: add axios API client with interceptors"
```

---

## Task 6: Create Reusable Components

**Files:**
- Create: `PayFrontend/src/components/StepIndicator.vue`
- Create: `PayFrontend/src/components/OrderInfoCard.vue`
- Create: `PayFrontend/src/components/StatusBadge.vue`
- Create: `PayFrontend/src/components/PaymentMethodSelector.vue`

- [ ] **Step 1: Create StepIndicator component**

Create `src/components/StepIndicator.vue`:
```vue
<template>
  <div class="step-indicator">
    <div
      v-for="step in 5"
      :key="step"
      :class="['step', { active: step === currentStep, completed: step < currentStep }]"
    >
      <div class="step-circle">
        <span v-if="step < currentStep">✓</span>
        <span v-else>{{ step }}</span>
      </div>
      <div class="step-label">Step {{ step }}</div>
    </div>
  </div>
</template>

<script setup>
const props = defineProps({
  currentStep: {
    type: Number,
    required: true
  }
})
</script>

<style scoped>
.step-indicator {
  display: flex;
  justify-content: space-between;
  margin-bottom: 40px;
  padding: 0 20px;
}

.step {
  display: flex;
  flex-direction: column;
  align-items: center;
  flex: 1;
  position: relative;
}

.step-circle {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  border: 2px solid #e0e0e0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: bold;
  background-color: #ffffff;
  margin-bottom: 8px;
}

.step.active .step-circle {
  border-color: #0066cc;
  color: #0066cc;
}

.step.completed .step-circle {
  background-color: #28a745;
  border-color: #28a745;
  color: #ffffff;
}

.step-label {
  font-size: 12px;
  color: #666666;
}

.step.active .step-label {
  color: #0066cc;
  font-weight: bold;
}
</style>
```

- [ ] **Step 2: Create OrderInfoCard component**

Create `src/components/OrderInfoCard.vue`:
```vue
<template>
  <div class="order-info-card">
    <h3>Order Information</h3>
    <div class="info-row">
      <span class="label">Order ID:</span>
      <span class="value">{{ orderId }}</span>
    </div>
    <div class="info-row">
      <span class="label">Product:</span>
      <span class="value">{{ productName }}</span>
    </div>
    <div class="info-row">
      <span class="label">Amount:</span>
      <span class="value">¥{{ amount.toFixed(2) }}</span>
    </div>
    <div v-if="description" class="info-row">
      <span class="label">Description:</span>
      <span class="value">{{ description }}</span>
    </div>
    <div v-if="status" class="info-row">
      <span class="label">Status:</span>
      <StatusBadge :status="status" />
    </div>
  </div>
</template>

<script setup>
import StatusBadge from './StatusBadge.vue'

const props = defineProps({
  orderId: String,
  productName: String,
  amount: Number,
  description: String,
  status: String
})
</script>

<style scoped>
.order-info-card {
  border: 1px solid #e0e0e0;
  border-radius: 8px;
  padding: 20px;
  margin-bottom: 20px;
  background-color: #f9f9f9;
}

.order-info-card h3 {
  margin-bottom: 16px;
  font-size: 18px;
}

.info-row {
  display: flex;
  justify-content: space-between;
  padding: 8px 0;
  border-bottom: 1px solid #e0e0e0;
}

.info-row:last-child {
  border-bottom: none;
}

.label {
  font-weight: bold;
  color: #666666;
}

.value {
  color: #000000;
}
</style>
```

- [ ] **Step 3: Create StatusBadge component**

Create `src/components/StatusBadge.vue`:
```vue
<template>
  <span :class="['status-badge', statusClass]">
    {{ displayText }}
  </span>
</template>

<script setup>
import { computed } from 'vue'

const props = defineProps({
  status: {
    type: String,
    required: true
  }
})

const displayText = computed(() => {
  return props.status.charAt(0).toUpperCase() + props.status.slice(1)
})

const statusClass = computed(() => {
  const status = props.status.toLowerCase()
  if (status === 'success' || status === 'paid') {
    return 'success'
  } else if (status === 'pending' || status === 'processing') {
    return 'pending'
  } else if (status === 'failed' || status === 'error') {
    return 'failed'
  }
  return 'info'
})
</script>

<style scoped>
.status-badge {
  padding: 4px 12px;
  border-radius: 4px;
  font-size: 14px;
  font-weight: bold;
}

.status-badge.success {
  background-color: #d4edda;
  color: #155724;
}

.status-badge.pending {
  background-color: #fff3cd;
  color: #856404;
}

.status-badge.failed {
  background-color: #f8d7da;
  color: #721c24;
}

.status-badge.info {
  background-color: #d1ecf1;
  color: #0c5460;
}
</style>
```

- [ ] **Step 4: Create PaymentMethodSelector component**

Create `src/components/PaymentMethodSelector.vue`:
```vue
<template>
  <div class="payment-method-selector">
    <h3>Select Payment Method</h3>
    <div class="options">
      <label
        v-for="method in methods"
        :key="method.value"
        :class="['option', { selected: modelValue === method.value }]"
      >
        <input
          type="radio"
          :value="method.value"
          :checked="modelValue === method.value"
          @change="$emit('update:modelValue', method.value)"
        />
        <span class="label">{{ method.label }}</span>
        <span class="description">{{ method.description }}</span>
      </label>
    </div>
  </div>
</template>

<script setup>
const props = defineProps({
  modelValue: String
})

defineEmits(['update:modelValue'])

const methods = [
  {
    value: 'qrcode',
    label: 'QR Code Payment',
    description: 'Scan with Alipay sandbox app'
  },
  {
    value: 'pc',
    label: 'PC Web Payment',
    description: 'Pay in browser on desktop'
  },
  {
    value: 'mobile',
    label: 'Mobile Web Payment',
    description: 'Mobile-optimized payment page'
  }
]
</script>

<style scoped>
.payment-method-selector {
  margin-bottom: 30px;
}

.payment-method-selector h3 {
  margin-bottom: 16px;
  font-size: 18px;
}

.options {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.option {
  display: flex;
  align-items: center;
  padding: 16px;
  border: 2px solid #e0e0e0;
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.2s;
}

.option:hover {
  border-color: #0066cc;
  background-color: #f5f5f5;
}

.option.selected {
  border-color: #0066cc;
  background-color: #e6f2ff;
}

.option input[type="radio"] {
  margin-right: 12px;
  width: 18px;
  height: 18px;
}

.option .label {
  font-weight: bold;
  margin-right: 12px;
  flex: 0 0 auto;
}

.option .description {
  color: #666666;
  font-size: 14px;
}
</style>
```

- [ ] **Step 5: Commit reusable components**

```bash
git add src/components
git commit -m "feat: add reusable UI components"
```

---

## Task 7: Implement Step 1 - Create Order

**Files:**
- Modify: `PayFrontend/src/views/StepView.vue`

- [ ] **Step 1: Implement Step 1 form**

Replace `src/views/StepView.vue` with:
```vue
<template>
  <div class="step-view">
    <StepIndicator :current-step="parseInt(stepNumber)" />

    <div v-if="stepNumber === '1'">
      <h2>Create Order</h2>
      <form @submit.prevent="handleCreateOrder" class="order-form">
        <div class="form-group">
          <label for="productName">Product Name *</label>
          <input
            id="productName"
            v-model="form.productName"
            type="text"
            required
            placeholder="Enter product name"
          />
        </div>

        <div class="form-group">
          <label for="amount">Amount (¥) *</label>
          <input
            id="amount"
            v-model.number="form.amount"
            type="number"
            step="0.01"
            min="0.01"
            required
            placeholder="0.00"
          />
        </div>

        <div class="form-group">
          <label for="description">Description</label>
          <textarea
            id="description"
            v-model="form.description"
            rows="3"
            placeholder="Enter product description (optional)"
          ></textarea>
        </div>

        <div v-if="error" class="error-message">{{ error }}</div>

        <div class="button-group">
          <button type="submit" :disabled="loading">
            {{ loading ? 'Creating...' : 'Create Order' }}
          </button>
          <button type="button" @click="handleReset" class="secondary">
            Reset
          </button>
        </div>
      </form>
    </div>

    <div v-else>
      <p>Other steps will be implemented in subsequent tasks</p>
    </div>
  </div>
</template>

<script setup>
import { ref } from 'vue'
import { useRouter } from 'vue-router'
import StepIndicator from '../components/StepIndicator.vue'
import { useOrderStore } from '../stores/order'
import { paymentApi } from '../api/payment'

const props = defineProps({
  stepNumber: {
    type: String,
    required: true
  }
})

const router = useRouter()
const orderStore = useOrderStore()

const form = ref({
  productName: '',
  amount: '',
  description: ''
})

const loading = ref(false)
const error = ref('')

async function handleCreateOrder() {
  loading.value = true
  error.value = ''

  try {
    const response = await paymentApi.createOrder({
      product_name: form.value.productName,
      total_amount: form.value.amount,
      description: form.value.description
    })

    orderStore.setOrder({
      orderId: response.out_trade_no,
      productName: form.value.productName,
      amount: form.value.amount,
      description: form.value.description,
      status: response.status || 'pending'
    })

    router.push('/step/2')
  } catch (err) {
    error.value = err.message || 'Failed to create order'
  } finally {
    loading.value = false
  }
}

function handleReset() {
  form.value = {
    productName: '',
    amount: '',
    description: ''
  }
  error.value = ''
}
</script>

<style scoped>
.step-view {
  max-width: 800px;
  margin: 0 auto;
}

h2 {
  margin-bottom: 24px;
  font-size: 24px;
}

.order-form {
  background-color: #ffffff;
  padding: 24px;
  border: 1px solid #e0e0e0;
  border-radius: 8px;
}

.form-group {
  margin-bottom: 20px;
}

.form-group label {
  display: block;
  margin-bottom: 8px;
  font-weight: bold;
  color: #000000;
}

.form-group input,
.form-group textarea {
  width: 100%;
  padding: 10px;
  border: 1px solid #e0e0e0;
  border-radius: 4px;
  font-size: 14px;
  font-family: inherit;
}

.form-group input:focus,
.form-group textarea:focus {
  outline: none;
  border-color: #0066cc;
}

.error-message {
  color: #dc3545;
  padding: 12px;
  background-color: #f8d7da;
  border: 1px solid #f5c6cb;
  border-radius: 4px;
  margin-bottom: 20px;
}

.button-group {
  display: flex;
  gap: 12px;
}

button {
  padding: 10px 20px;
  border: none;
  border-radius: 4px;
  font-size: 14px;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.2s;
}

button[type="submit"] {
  background-color: #0066cc;
  color: #ffffff;
}

button[type="submit"]:hover:not(:disabled) {
  background-color: #0052a3;
}

button[type="submit"]:disabled {
  background-color: #cccccc;
  cursor: not-allowed;
}

button.secondary {
  background-color: #ffffff;
  color: #000000;
  border: 1px solid #e0e0e0;
}

button.secondary:hover {
  background-color: #f5f5f5;
}
</style>
```

- [ ] **Step 2: Test Step 1**

```bash
npm run dev
```

Expected:
- Form displays at http://localhost:5173/step/1
- Form validation works
- Submit button shows loading state
- On success, redirects to /step/2

- [ ] **Step 3: Commit Step 1 implementation**

```bash
git add src/views/StepView.vue
git commit -m "feat: implement Step 1 - create order form"
```

---

## Task 8: Implement Step 2 - Payment Method Selection

**Files:**
- Modify: `PayFrontend/src/views/StepView.vue`

- [ ] **Step 1: Add Step 2 template**

Update the template section in `src/views/StepView.vue`, replace the "v-else" block with:

```vue
    <div v-else-if="stepNumber === '2'">
      <h2>Select Payment Method</h2>
      <OrderInfoCard
        :order-id="orderStore.orderId"
        :product-name="orderStore.productName"
        :amount="orderStore.amount"
        :description="orderStore.description"
      />

      <PaymentMethodSelector
        v-model="paymentMethod"
        @update:modelValue="handlePaymentMethodChange"
      />

      <div v-if="error" class="error-message">{{ error }}</div>

      <div class="button-group">
        <button @click="handleConfirm" :disabled="!paymentMethod || loading">
          {{ loading ? 'Processing...' : 'Confirm & Pay' }}
        </button>
        <button @click="handleBack" class="secondary">
          Back
        </button>
      </div>
    </div>

    <div v-else>
      <p>Steps 3-5 will be implemented in subsequent tasks</p>
    </div>
```

- [ ] **Step 2: Add Step 2 script logic**

Add these imports and variables to the script section:
```javascript
import OrderInfoCard from '../components/OrderInfoCard.vue'
import PaymentMethodSelector from '../components/PaymentMethodSelector.vue'
import { usePaymentStore } from '../stores/payment'

const paymentStore = usePaymentStore()
const paymentMethod = ref('')
```

Add these functions:
```javascript
function handlePaymentMethodChange(method) {
  paymentMethod.value = method
}

async function handleConfirm() {
  if (!paymentMethod.value) {
    error.value = 'Please select a payment method'
    return
  }

  loading.value = true
  error.value = ''

  try {
    paymentStore.setPaymentMethod(paymentMethod.value)

    // Navigate to payment result page
    router.push('/step/3')
  } catch (err) {
    error.value = err.message || 'Failed to initiate payment'
  } finally {
    loading.value = false
  }
}

function handleBack() {
  router.push('/step/1')
}
```

- [ ] **Step 3: Test Step 2**

Expected:
- Order info displays correctly
- Payment method selector works
- Back button returns to Step 1
- Confirm button advances to Step 3

- [ ] **Step 4: Commit Step 2 implementation**

```bash
git add src/views/StepView.vue
git commit -m "feat: implement Step 2 - payment method selection"
```

---

## Task 9: Implement Step 3 - Payment Result

**Files:**
- Modify: `PayFrontend/src/views/StepView.vue`

- [ ] **Step 1: Add Step 3 template**

Replace the "Steps 3-5" block with:

```vue
    <div v-else-if="stepNumber === '3'">
      <h2>Payment Result</h2>
      <OrderInfoCard
        :order-id="orderStore.orderId"
        :product-name="orderStore.productName"
        :amount="orderStore.amount"
        :status="paymentStore.paymentStatus"
      />

      <div v-if="paymentStore.paymentStatus === 'success'" class="success-message">
        <h3>✓ Payment Successful</h3>
        <p>Your payment has been completed successfully.</p>
      </div>

      <div v-else-if="paymentStore.paymentStatus === 'failed'" class="error-message">
        <h3>✗ Payment Failed</h3>
        <p>{{ paymentError }}</p>
      </div>

      <div v-else class="info-message">
        <h3>Waiting for Payment...</h3>
        <p>Please complete the payment using your selected method.</p>
        <button @click="handleQueryStatus" :disabled="loading">
          {{ loading ? 'Checking...' : 'Refresh Status' }}
        </button>
      </div>

      <div class="button-group" v-if="paymentStore.paymentStatus === 'success'">
        <button @click="handleNext">
          Next: Refund
        </button>
      </div>
    </div>

    <div v-else-if="stepNumber === '4'">
      <p>Step 4 will be implemented in next task</p>
    </div>

    <div v-else-if="stepNumber === '5'">
      <p>Step 5 will be implemented in next task</p>
    </div>
```

- [ ] **Step 2: Add Step 3 script logic**

Add these variables:
```javascript
const paymentError = ref('')
const pollingInterval = ref(null)
```

Add these functions:
```javascript
async function handleQueryStatus() {
  loading.value = true
  paymentError.value = ''

  try {
    const response = await paymentApi.queryOrder(orderStore.orderId)

    paymentStore.setPaymentInfo({
      status: response.trade_status || 'pending',
      qrCodeUrl: response.qr_code_url || '',
      paymentUrl: response.pay_url || ''
    })

    orderStore.setOrder({
      ...orderStore.$state,
      status: response.trade_status || 'pending'
    })

    // If payment successful, clear polling
    if (response.trade_status === 'success') {
      if (pollingInterval.value) {
        clearInterval(pollingInterval.value)
        pollingInterval.value = null
      }
    }
  } catch (err) {
    paymentError.value = err.message || 'Failed to query payment status'
  } finally {
    loading.value = false
  }
}

function handleNext() {
  router.push('/step/4')
}

// Start polling when entering Step 3
onMounted(() => {
  if (props.stepNumber === '3') {
    orderStore.loadFromSession()

    // Initial query
    handleQueryStatus()

    // Poll every 5 seconds
    pollingInterval.value = setInterval(() => {
      if (paymentStore.paymentStatus !== 'success') {
        handleQueryStatus()
      } else {
        clearInterval(pollingInterval.value)
      }
    }, 5000)
  }
})

// Clear polling when leaving Step 3
onUnmounted(() => {
  if (pollingInterval.value) {
    clearInterval(pollingInterval.value)
  }
})
```

Add this import:
```javascript
import { onMounted, onUnmounted } from 'vue'
```

Add these styles:
```css
.success-message {
  padding: 16px;
  background-color: #d4edda;
  border: 1px solid #c3e6cb;
  border-radius: 4px;
  margin-bottom: 20px;
}

.success-message h3 {
  color: #155724;
  margin-bottom: 8px;
}

.info-message {
  padding: 16px;
  background-color: #d1ecf1;
  border: 1px solid #bee5eb;
  border-radius: 4px;
  margin-bottom: 20px;
}

.info-message h3 {
  color: #0c5460;
  margin-bottom: 8px;
}

.info-message button {
  margin-top: 12px;
}
```

- [ ] **Step 3: Test Step 3**

Expected:
- Displays payment status
- Auto-refreshes every 5 seconds
- Shows success when payment complete
- Enables "Next" button when successful

- [ ] **Step 4: Commit Step 3 implementation**

```bash
git add src/views/StepView.vue
git commit -m "feat: implement Step 3 - payment result with polling"
```

---

## Task 10: Implement Step 4 - Refund

**Files:**
- Modify: `PayFrontend/src/views/StepView.vue`

- [ ] **Step 1: Add Step 4 template**

Replace the Step 4 placeholder block with:

```vue
    <div v-else-if="stepNumber === '4'">
      <h2>Request Refund</h2>
      <OrderInfoCard
        :order-id="orderStore.orderId"
        :product-name="orderStore.productName"
        :amount="orderStore.amount"
        :status="orderStore.status"
      />

      <form @submit.prevent="handleRefund" class="order-form">
        <div class="form-group">
          <label for="refundAmount">Refund Amount (¥) *</label>
          <input
            id="refundAmount"
            v-model.number="refundForm.amount"
            type="number"
            step="0.01"
            min="0.01"
            :max="orderStore.amount"
            required
            placeholder="0.00"
          />
          <small>Maximum refundable: ¥{{ orderStore.amount.toFixed(2) }}</small>
        </div>

        <div class="form-group">
          <label for="refundReason">Reason</label>
          <textarea
            id="refundReason"
            v-model="refundForm.reason"
            rows="3"
            placeholder="Enter refund reason (optional)"
          ></textarea>
        </div>

        <div v-if="error" class="error-message">{{ error }}</div>

        <div class="button-group">
          <button type="submit" :disabled="loading">
            {{ loading ? 'Processing...' : 'Submit Refund' }}
          </button>
          <button @click="handleBack" class="secondary">
            Back
          </button>
        </div>
      </form>
    </div>
```

- [ ] **Step 2: Add Step 4 script logic**

Add these variables:
```javascript
const refundForm = ref({
  amount: orderStore.amount,
  reason: ''
})
```

Add these functions:
```javascript
async function handleRefund() {
  loading.value = true
  error.value = ''

  try {
    const response = await paymentApi.refund({
      out_trade_no: orderStore.orderId,
      refund_amount: refundForm.value.amount,
      refund_reason: refundForm.value.reason
    })

    paymentStore.setRefundInfo({
      amount: refundForm.value.amount,
      reason: refundForm.value.reason,
      status: response.refund_status || 'pending'
    })

    router.push('/step/5')
  } catch (err) {
    error.value = err.message || 'Failed to submit refund'
  } finally {
    loading.value = false
  }
}

function handleBack() {
  router.push('/step/3')
}
```

- [ ] **Step 3: Test Step 4**

Expected:
- Pre-fills with full refund amount
- Validates amount ≤ original amount
- On success, advances to Step 5

- [ ] **Step 4: Commit Step 4 implementation**

```bash
git add src/views/StepView.vue
git commit -m "feat: implement Step 4 - refund form"
```

---

## Task 11: Implement Step 5 - Refund Result

**Files:**
- Modify: `PayFrontend/src/views/StepView.vue`

- [ ] **Step 1: Add Step 5 template**

Replace the Step 5 placeholder block with:

```vue
    <div v-else-if="stepNumber === '5'">
      <h2>Refund Result</h2>
      <OrderInfoCard
        :order-id="orderStore.orderId"
        :product-name="orderStore.productName"
        :amount="orderStore.amount"
        :status="orderStore.status"
      />

      <div class="refund-info-card">
        <h3>Refund Information</h3>
        <div class="info-row">
          <span class="label">Refund Amount:</span>
          <span class="value">¥{{ paymentStore.refundAmount.toFixed(2) }}</span>
        </div>
        <div v-if="paymentStore.refundReason" class="info-row">
          <span class="label">Reason:</span>
          <span class="value">{{ paymentStore.refundReason }}</span>
        </div>
        <div class="info-row">
          <span class="label">Status:</span>
          <StatusBadge :status="paymentStore.refundStatus" />
        </div>
      </div>

      <div v-if="paymentStore.refundStatus === 'success'" class="success-message">
        <h3>✓ Refund Successful</h3>
        <p>Your refund has been processed successfully.</p>
      </div>

      <div v-else-if="paymentStore.refundStatus === 'failed'" class="error-message">
        <h3>✗ Refund Failed</h3>
        <p>{{ refundError }}</p>
      </div>

      <div v-else class="info-message">
        <h3>Processing Refund...</h3>
        <p>Your refund is being processed.</p>
        <button @click="handleQueryRefund" :disabled="loading">
          {{ loading ? 'Checking...' : 'Refresh Status' }}
        </button>
      </div>

      <div class="button-group" v-if="paymentStore.refundStatus === 'success'">
        <button @click="handleFinish">
          Finish Test
        </button>
      </div>
    </div>
```

- [ ] **Step 2: Add Step 5 script logic**

Add these variables:
```javascript
const refundError = ref('')
const refundPollingInterval = ref(null)
```

Add these functions:
```javascript
async function handleQueryRefund() {
  loading.value = true
  refundError.value = ''

  try {
    const response = await paymentApi.queryRefund(orderStore.orderId)

    paymentStore.setRefundInfo({
      amount: paymentStore.refundAmount,
      reason: paymentStore.refundReason,
      status: response.refund_status || 'pending'
    })

    // If refund successful, clear polling
    if (response.refund_status === 'success') {
      if (refundPollingInterval.value) {
        clearInterval(refundPollingInterval.value)
        refundPollingInterval.value = null
      }
    }
  } catch (err) {
    refundError.value = err.message || 'Failed to query refund status'
  } finally {
    loading.value = false
  }
}

function handleFinish() {
  // Clear all stores
  orderStore.clear()
  paymentStore.clear()

  // Return to Step 1
  router.push('/step/1')
}

// Update onMounted to handle Step 5
onMounted(() => {
  if (props.stepNumber === '3') {
    // Step 3 logic (already implemented)
    orderStore.loadFromSession()
    handleQueryStatus()

    pollingInterval.value = setInterval(() => {
      if (paymentStore.paymentStatus !== 'success') {
        handleQueryStatus()
      } else {
        clearInterval(pollingInterval.value)
      }
    }, 5000)
  } else if (props.stepNumber === '5') {
    // Step 5 logic
    orderStore.loadFromSession()

    // Initial query
    handleQueryRefund()

    // Poll every 5 seconds
    refundPollingInterval.value = setInterval(() => {
      if (paymentStore.refundStatus !== 'success') {
        handleQueryRefund()
      } else {
        clearInterval(refundPollingInterval.value)
      }
    }, 5000)
  }
})

// Update onUnmounted to clear both intervals
onUnmounted(() => {
  if (pollingInterval.value) {
    clearInterval(pollingInterval.value)
  }
  if (refundPollingInterval.value) {
    clearInterval(refundPollingInterval.value)
  }
})
```

Add these styles:
```css
.refund-info-card {
  border: 1px solid #e0e0e0;
  border-radius: 8px;
  padding: 20px;
  margin-bottom: 20px;
  background-color: #f9f9f9;
}

.refund-info-card h3 {
  margin-bottom: 16px;
  font-size: 18px;
}
```

- [ ] **Step 3: Test Step 5**

Expected:
- Displays refund information
- Polls for refund status
- "Finish Test" clears state and returns to Step 1

- [ ] **Step 4: Commit Step 5 implementation**

```bash
git add src/views/StepView.vue
git commit -m "feat: implement Step 5 - refund result with polling"
```

---

## Task 12: Create Docker Configuration

**Files:**
- Create: `PayFrontend/Dockerfile`
- Create: `PayFrontend/nginx.conf`
- Modify: `docker-compose.yml`

- [ ] **Step 1: Create Dockerfile**

Create `PayFrontend/Dockerfile`:
```dockerfile
# Build stage
FROM node:20-alpine AS builder

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .
RUN npm run build

# Production stage
FROM nginx:alpine

COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

- [ ] **Step 2: Create nginx.conf**

Create `PayFrontend/nginx.conf`:
```nginx
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # API proxy to backend
    location /api {
        proxy_pass http://backend:8088;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

- [ ] **Step 3: Update root docker-compose.yml**

Add frontend service to `docker-compose.yml`:
```yaml
services:
  backend:
    build: ./PayBackend
    ports:
      - "8088:8088"
    environment:
      - POSTGRES_HOST=postgres
      - REDIS_HOST=redis
    volumes:
      - ./PayBackend:/app
      - ./PayBackend/certs:/app/certs
    depends_on:
      - postgres
      - redis

  frontend:
    build: ./PayFrontend
    ports:
      - "80:80"
    depends_on:
      - backend

  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: payplugin
      POSTGRES_USER: payplugin
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-123456}
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    command: redis-server --requirepass ${REDIS_PASSWORD:-123456}
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
```

- [ ] **Step 4: Test Docker build**

```bash
cd PayFrontend
docker build -t pay-frontend .
```

Expected: Build completes successfully

- [ ] **Step 5: Commit Docker configuration**

```bash
git add PayFrontend/Dockerfile PayFrontend/nginx.conf docker-compose.yml
git commit -m "feat: add Docker configuration for frontend"
```

---

## Task 13: Update Documentation

**Files:**
- Modify: `README.md`
- Modify: `PayFrontend/README.md`

- [ ] **Step 1: Update root README.md**

Add to `README.md`:
```markdown
## Quick Start

### Using Docker Compose (Recommended)

\`\`\`bash
docker-compose up -d
\`\`\`

Access the frontend at http://localhost

### Manual Development

**Backend:**
\`\`\`bash
cd PayBackend
./build/Release/payplugin
\`\`\`

**Frontend:**
\`\`\`bash
cd PayFrontend
npm install
npm run dev
\`\`\`

Access frontend at http://localhost:5173
\`\`\`

## Architecture

- **PayBackend**: Drogon-based payment backend with Alipay sandbox integration
- **PayFrontend**: Vue 3 test interface for payment flows

See [PayFrontend/README.md](PayFrontend/README.md) for frontend details.
```

- [ ] **Step 2: Update PayFrontend/README.md**

Replace `PayFrontend/README.md` with:
```markdown
# Payment Test Frontend

Vue 3 test interface for Alipay sandbox payment flows.

## Features

- **Wizard-style workflow**: 5-step guided process
- **Multiple payment methods**: QR code, PC web, mobile web
- **Real-time status**: Auto-refresh payment and refund status
- **Session persistence**: State survives page refreshes
- **Minimalist UI**: Clean, functional interface

## Development Setup

\`\`\`bash
npm install
npm run dev
\`\`\`

Access at http://localhost:5173

The Vite dev server proxies API requests to the backend at `http://localhost:8088`.

## Build

\`\`\`bash
npm run build
\`\`\`

Output: `dist/`

## Docker

\`\`\`bash
docker build -t pay-frontend .
\`\`\`

Or use docker-compose from the project root:

\`\`\`bash
docker-compose up frontend
\`\`\`

## Testing Workflow

1. **Step 1**: Create order with product name and amount
2. **Step 2**: Select payment method (QR code/PC/Mobile)
3. **Step 3**: Complete payment (status auto-refreshes)
4. **Step 4**: Request refund (partial or full)
5. **Step 5**: View refund status

## Project Structure

\`\`\`
src/
├── api/           # API client (axios)
├── components/    # Reusable UI components
├── router/        # Vue Router configuration
├── stores/        # Pinia state management
├── utils/         # Helper functions
├── views/         # Page components
├── App.vue        # Root component
└── main.js        # Entry point
\`\`\`

## Environment

No environment variables required for development. The backend API URL is configured in `vite.config.js`.

For production, ensure the backend is accessible at `/api` relative to the frontend.
```

- [ ] **Step 3: Commit documentation updates**

```bash
git add README.md PayFrontend/README.md
git commit -m "docs: update README with frontend setup instructions"
```

---

## Task 14: Final Testing and Integration

**Files:**
- No new files

- [ ] **Step 1: Test complete flow**

```bash
# Start backend
cd PayBackend
./build/Release/payplugin

# In another terminal, start frontend
cd PayFrontend
npm run dev
```

Test the complete flow:
1. Create order
2. Select payment method
3. Complete payment
4. Request refund
5. View refund status

- [ ] **Step 2: Test Docker Compose**

```bash
cd ..
docker-compose up
```

Access http://localhost and verify:
- Frontend loads
- API calls work
- All steps functional

- [ ] **Step 3: Test session persistence**

1. Complete Step 1
2. Refresh page
3. Verify order info persists

- [ ] **Step 4: Test error handling**

Test scenarios:
- Invalid form input
- Backend offline (show error message)
- Payment failure
- Refund failure

- [ ] **Step 5: Final commit**

```bash
cd PayFrontend
git add .
git commit -m "test: complete integration testing"
```

---

## Task 15: Merge Frontend to Main Project

**Files:**
- No new files

- [ ] **Step 1: Verify all commits**

```bash
cd PayFrontend
git log --oneline
```

Expected: Clean commit history with all tasks

- [ ] **Step 2: Remove .git from PayFrontend**

```bash
rm -rf .git
```

- [ ] **Step 3: Commit to main project**

```bash
cd ..
git add PayFrontend
git add docker-compose.yml
git add README.md
git commit -m "feat: add payment test frontend (Vue 3 + Vite)

- Implement 5-step wizard workflow
- Support multiple payment methods (QR code, PC, mobile)
- Real-time status polling
- Session persistence
- Docker Compose integration

Closes #frontend-implementation"
```

- [ ] **Step 4: Push to remote**

```bash
git push origin master
```

---

## Self-Review Checklist

After completing all tasks, verify:

- [ ] All 5 wizard steps implemented and functional
- [ ] API integration works with backend
- [ ] Session persistence functions correctly
- [ ] Error handling displays user-friendly messages
- [ ] Docker Compose launches both services
- [ ] Documentation is complete and accurate
- [ ] Code follows Vue 3 best practices
- [ ] No console errors in browser
- [ ] Responsive design works on desktop (minimal mobile support acceptable)

---

## Completion Criteria

The implementation is complete when:

1. **User can create an order** → API returns order ID
2. **User can select payment method** → Navigates to payment result
3. **Payment status updates** → Auto-refreshes every 5 seconds
4. **User can request refund** → API accepts refund request
5. **Refund status updates** → Auto-refreshes every 5 seconds
6. **Session persists** → Page refresh maintains state
7. **Docker Compose works** → Single command starts full stack
8. **Documentation is clear** → Developer can set up and run

---

**End of Implementation Plan**
