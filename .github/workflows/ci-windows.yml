name: Windows CI

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

env:
  BUILD_TYPE: Release

jobs:
  build-and-test:
    runs-on: windows-2022
    # Cold Conan cache builds drogon/openssl from source, which can take a while
    timeout-minutes: 120

    steps:
      - uses: actions/checkout@v4

      - name: Install Conan
        run: |
          pip install conan
          conan profile detect --force

      # --- Conan dependency cache ---
      # Drogon 1.9.13, OpenSSL 3.5.7 and friends come from Conan now; cache the
      # package store so they are only built from source when the lockfile changes.
      - name: Cache Conan packages
        uses: actions/cache@v4
        with:
          path: ~/.conan2/p
          key: ${{ runner.os }}-conan2-${{ hashFiles('PayBackend/conanfile.py', 'PayBackend/conan.lock') }}
          restore-keys: |
            ${{ runner.os }}-conan2-

      - name: Install dependencies (Conan)
        working-directory: ${{github.workspace}}/PayBackend
        run: conan install . --output-folder=build/windows-msvc -s build_type=${{env.BUILD_TYPE}} -s compiler.cppstd=17 --build=missing

      - name: Configure (cmake --preset)
        working-directory: ${{github.workspace}}/PayBackend
        run: cmake --preset windows-msvc

      - name: Build (cmake --build --preset)
        working-directory: ${{github.workspace}}/PayBackend
        run: cmake --build --preset windows-msvc

      - name: Upload test logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: windows-test-logs
          path: |
            ${{github.workspace}}/PayBackend/build/windows-msvc/Testing/
            ${{github.workspace}}/PayBackend/logs/
          retention-days: 7
